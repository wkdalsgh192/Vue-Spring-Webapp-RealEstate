<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.happyhouse.model.mapper.MemberMapper">
	<select id="selectLogin" parameterType="MemberDto" resultType="MemberDto">
		select *
		from member
		where id = #{id} and pw = #{pw}
	</select>
	
	<insert id="insertsigup" parameterType="MemberDto">
		insert into member(id, pw, email)
		values (#{id}, #{pw}, #{email})
	</insert>
	
	<update id="update" parameterType="MemberDto">
		update member
		set pw = #{pw}, email = #{email}
		where id = #{id}
	</update>
	
	<delete id="delete" parameterType="String">
		delete from member
		where id = #{id}
	</delete>
	
</mapper>